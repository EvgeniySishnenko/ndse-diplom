const UserModel = require("../models/user");

class UserService {
  async signup({ email, password, name, contactPhone }) {
    try {
    } catch (error) {}
  }
  async signin({ email, password, name, contactPhone }) {}
}
